# qdrant

Qdrant runs with the stock image. Storage persists in the `qdrant-data` volume.
Dashboard: http://localhost:6333/dashboard

Collections (project files, docs, conversation embeddings) are created by the
RAG layer in Phase 6.
